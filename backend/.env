# Storage mode: "json" (local files, no MongoDB needed) or "mongo"
STORAGE_MODE=json

# MongoDB (only needed if STORAGE_MODE=mongo)
MONGODB_URI=mongodb://localhost:27017

# Cardano Preprod Testnet — get a FREE preprod key from https://blockfrost.io
# IMPORTANT: You need a PREPROD key, not mainnet!
BLOCKFROST_PROJECT_ID=preprodeFmgdlWiR2oxl2o4N6hxbeIhsiPPasqM
CARDANO_SKEY_FILE=admin.skey
